export interface Actualite {
    success: boolean,
    /*actualites: [{
        id: number,
        denomination: string,
        contenu: string,
        image: string
    }],*/
    actualites: any,
    message: string;
}